# experiments/phase1/N08.yaml
#
# id: N08
# phase: phase1
# fixture/dataset: CleanControl + NP-SYNTAX
# analysis.type: capability_probe
#
# exit code 与 V1/V9/V10 语义。导出 command-capabilities.json 与 exit_code_policy.json。N10 rc 延迟回填。
#
# 本文件将包含严格有序的 steps、cache_state、command、repeat、hooks、exports。
# 当前为骨架，不含可执行步骤。
