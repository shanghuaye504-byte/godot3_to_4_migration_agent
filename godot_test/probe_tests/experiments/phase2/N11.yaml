# experiments/phase2/N11.yaml
#
# id: N11
# phase: phase2
# fixture/dataset: CP-BIGFILE
# analysis.type: transform_diff
#
# 大文件跳过/阈值/hang。依赖 N15。big.gd/longline.gd 由 generate_big_file 生成且必须含可转换模式。
#
# 本文件将包含严格有序的 steps、cache_state、command、repeat、hooks、exports。
# 当前为骨架，不含可执行步骤。
