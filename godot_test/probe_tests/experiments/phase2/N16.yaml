# experiments/phase2/N16.yaml
#
# id: N16
# phase: phase2
# fixture/dataset: CP-MINIMAL + CP-*
# analysis.type: capability_probe
#
# ProjectUpgradeTool 与 converter/--import 的职责矩阵。三组隔离工作区。
#
# 本文件将包含严格有序的 steps、cache_state、command、repeat、hooks、exports。
# 当前为骨架，不含可执行步骤。
