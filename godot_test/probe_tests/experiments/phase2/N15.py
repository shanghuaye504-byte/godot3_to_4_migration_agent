"""N15 采集脚本：converter 与 ProjectUpgradeTool 的 CLI 能力门。P2-1。

脚本只采集；能力与职责矩阵交给 analyzer/capability.py（无对错判定）。
本文件只记录契约，不含实现。

================================================================
1. 身份与依赖
================================================================

N = "N15"
依赖：无（第二阶段能力门，不依赖第一阶段实验脚本）
repeat = 1（纯能力探测）
导出：artifacts/latest/N15.json（converter-capabilities）
判定：python analyzer/capability.py artifacts/<run-id>/N15/

N21 启动时必须读本导出；能力门失败时 N21 标记为 BLOCKED，不得静默跳过。
原 N16 已并入本卡片。
不能只根据源码里存在对应代码就判定 CLI 可用。

================================================================
2. fixture / derived
================================================================

fixture：fixtures/phase2/CP-MINIMAL（最小 3.x 项目，config_version=4）
无 derived patch。
步骤 2–5 必须各用一个独立副本，同一工作区连续执行会让文件变更无法归因。
不修改任何原始项目。

================================================================
3. 步骤表（一个 measure 对应一行）
================================================================

  1  --help                         不涉及项目     ×1   convert / 3to4 / upgrade 相关入口是否存在
                                                         记完整 help、版本、build hash
  2  --validate-conversion-3to4     副本 A         ×1   能否对最小 config_version=4 项目调用成功
  3  --convert-3to4                 副本 B         ×1   是否真的改了文件（以 diff 为准，不看 stdout）
  4  upgrade tool（若步骤 1 发现入口） 副本 C      ×1   它改哪些文件类型：GDScript、UID、.tscn、.tres、资源路径、import cache
  5  V3                             副本 D COLD    ×1   用 --import 近似替代 upgrade tool 时的边界在哪

================================================================
4. finally 清理
================================================================

杀进程组、删全部独立副本、校验原 CP-MINIMAL 仍 clean。
"""
