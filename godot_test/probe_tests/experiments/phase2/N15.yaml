# experiments/phase2/N15.yaml
#
# id: N15
# phase: phase2
# fixture/dataset: CP-MINIMAL
# analysis.type: capability_probe
#
# converter CLI 能力门。导出 converter-capabilities.json。失败则后续 phase2 全部 BLOCKED。
#
# 本文件将包含严格有序的 steps、cache_state、command、repeat、hooks、exports。
# 当前为骨架，不含可执行步骤。
