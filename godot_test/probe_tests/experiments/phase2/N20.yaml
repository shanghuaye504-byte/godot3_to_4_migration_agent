# experiments/phase2/N20.yaml
#
# id: N20
# phase: phase2
# fixture/dataset: CP-MUTATION
# analysis.type: transform_diff
#
# mutation 评测泄漏。每算子独立工作区；先 downgrade_config_version 再施加单个变异。
#
# 本文件将包含严格有序的 steps、cache_state、command、repeat、hooks、exports。
# 当前为骨架，不含可执行步骤。
