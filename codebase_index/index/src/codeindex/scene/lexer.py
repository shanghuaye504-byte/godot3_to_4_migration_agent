"""L0：场景文本的词法。

尚未实现。验收见 codebase_index/NEXT_STEP.md 的 Step 1。

以后负责：
- 把 .tscn / .tres 切成带 span 的值（字符串、数字、调用、数组、字典）。
- 多行值用括号深度切开，深度统计跳过字符串里的括号。
- 没见过的构造器保留为 call，不报语法错误。
- 不判断类型是否存在，不读磁盘，不调用 Godot。
"""
