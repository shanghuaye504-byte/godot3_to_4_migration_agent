---
alwaysApply: true
---
请你在进行工作时不论是生成代码还是生成技术文档或是做架构规划和函数接口设计，数据库设计等工作时，一定要及时的将你的设计方案，中间的关键设计过程记录下来，具体的md文档存放位置为就近存放在当前子目录的docs/下面，这个文件夹不论是对任何目录都是gitignore的，用于给开发者看，同时也用于给你自己进行参考，尤其是避免在上下文压缩时丢失关键信息。

项目的整体环境配置分层次，一层是根目录下进行一个统一的uv环境管理，这个是暴露给最终用户的，在每一个子目录下都可以有一个uv环境管理开发和build阶段，通过依赖分组，最终只暴露给外面这个包要打包输出的东西，或者子目录的子目录也可以，但是一般情况不要嵌套。